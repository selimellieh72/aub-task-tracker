# Storage layer for the Task Tracker (per ADR-001):
# in-memory dict backed by a JSON file (tasks.json) for persistence.
# Load/save helpers will be implemented alongside the CRUD endpoints.
# tasks.json is created at runtime and is gitignored.
